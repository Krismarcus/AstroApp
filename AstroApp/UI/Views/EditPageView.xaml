<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AstroApp.UI.Views.EditPageView"
             xmlns:views="clr-namespace:AstroApp.UI.Views"
             xmlns:controls="clr-namespace:AstroApp.UI.Controls"
             x:DataType="views:CalendarView">
    <ScrollView>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Text="{Binding MonthName}" HorizontalOptions="Center" VerticalOptions="Center"/>
            <VerticalStackLayout Grid.Row="1" x:Name="EventList"/>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Text="Previous" Clicked="PrevButton_Clicked"/>
                <Button Grid.Column="1" Text="Next" Clicked="NextButton_Clicked"/>
            </Grid>
            <!--<Grid Grid.Row="3">
                <Button Text="Save" Clicked="SaveButton_Clicked"/>
            </Grid>-->
        </Grid>
    </ScrollView>
</ContentView>
