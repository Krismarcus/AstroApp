<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AstroApp.UI.Pages.AdminPage"
             xmlns:pages="clr-namespace:AstroApp.UI.Pages"
             xmlns:controls="clr-namespace:AstroApp.UI.Controls"
             x:DataType="pages:AdminPage"
             Title="AdminPage">    
        <ScrollView>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Text="Previous" Clicked="PrevButton_Clicked"/>
                    <Button Grid.Column="1" Text="Next" Clicked="NextButton_Clicked"/>
                </Grid>
                <Label Grid.Row="1" Text="{Binding MonthName}" FontSize="Header" HorizontalOptions="Center" VerticalOptions="Center"/>           
                <Picker Grid.Row="2" x:Name="MoonInZodiacPicker" Title="Moon Zodiac" SelectedIndexChanged="ZodiacSignPicker_OnSelectedIndexChanged"/>           
                <VerticalStackLayout Grid.Row="3" x:Name="EventList"/>
                <Grid Grid.Row="4">
                    <Button Text="Save" Clicked="SaveButton_Clicked"/>
                </Grid>
            </Grid>
        </ScrollView>    
</ContentPage>